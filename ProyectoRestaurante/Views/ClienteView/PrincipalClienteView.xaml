<UserControl x:Class="ProyectoRestaurante.Views.ClienteView.PrincipalClienteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ProyectoRestaurante.ViewModels"
             xmlns:local="clr-namespace:ProyectoRestaurante.Views.ClienteView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:MenuViewModel></vm:MenuViewModel>
    </UserControl.DataContext>
    <UserControl.Resources>
        <local:MenuView x:Key="VerMenu"></local:MenuView>
    </UserControl.Resources>
    <ContentControl Content="{StaticResource VerMenu}">
            
    </ContentControl>
</UserControl>
