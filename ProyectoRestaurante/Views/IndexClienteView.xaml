<UserControl x:Class="ProyectoRestaurante.Views.IndexClienteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ProyectoRestaurante.Views"
             xmlns:views="clr-namespace:ProyectoRestaurante.Views.ClienteView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <views:MenuView x:Key="MenuView"></views:MenuView>
    </UserControl.Resources>
    <ContentControl>
        <ContentControl.Style>
            <Style TargetType="ContentControl">
                <Setter Property="Content" Value="{StaticResource MenuView}"></Setter>
            </Style>
        </ContentControl.Style>
    </ContentControl>
</UserControl>
